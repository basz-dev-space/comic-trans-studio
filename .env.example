# Example environment variables for comic-trans-studio
# Copy this file to `.env` and fill in appropriate values before running the app.

# Primary database URL (Postgres example)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://dbuser:dbpass@localhost:5432/comictrans?schema=public

# Alternative: Prisma-specific env var names that the app will check
PRISMA_DATABASE_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL=
PGDATABASE_URL=

# Support reading DB URL from a file (the app looks for *_FILE names)
# DATABASE_URL_FILE=/run/secrets/DATABASE_URL
# PRISMA_DATABASE_URL_FILE=/run/secrets/PRISMA_DATABASE_URL

# Or use a local SQLite DB for quick local dev
# DATABASE_URL=file:./dev.db

# Runtime settings
NODE_ENV=development
PORT=5173

# Session TTL (seconds) - used by the in-memory fallback only; set short for tests
SESSION_TTL_SECONDS=86400

# Admin management credentials (use only for local/admin UI; prefer secrets in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
